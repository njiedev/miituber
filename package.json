{
  "name": "miituber",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "dev:landing": "vite --config vite.landing.config.ts",
    "build:landing": "tsc && vite build --config vite.landing.config.ts",
    "verify:renderer-glb": "node scripts/verify-renderer-glb.mjs",
    "test": "vitest run",
    "preview": "npm run build && wrangler dev",
    "tauri": "tauri",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@types/three": "^0.184.1",
    "ffl.js": "github:ariankordi/FFL.js#v2.2.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.43.0",
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.107.0"
  }
}
